# ===========================================
# Makefile для Telegram Bot проекта
# Автоматизация всех задач разработки
# ===========================================

# Переменные
PYTHON := python3
PIP := pip3
POETRY := poetry
DOCKER := docker
DOCKER_COMPOSE := docker-compose
PROJECT_NAME := telegram-bot

# Цвета для вывода
RED := \033[0;31m
GREEN := \033[0;32m
YELLOW := \033[1;33m
BLUE := \033[0;34m
NC := \033[0m # No Color

# Файлы и директории
MAIN_FILE := main.py
REQUIREMENTS_FILE := requirements.txt
DOCKER_COMPOSE_FILE := docker-compose.yml
ENV_FILE := .env

# ==================== HELP ====================
.PHONY: help
help: ## 📖 Показать справку
	@echo "$(BLUE)🤖 Telegram Bot - Команды разработки$(NC)"
	@echo ""
	@echo "$(GREEN)🚀 ОСНОВНЫЕ КОМАНДЫ:$(NC)"
	@awk 'BEGIN {FS = ":.*?## "} /^[a-zA-Z_-]+:.*?## / {printf "  $(YELLOW)%-20s$(NC) %s\n", $$1, $$2}' $(MAKEFILE_LIST)
	@echo ""
	@echo "$(GREEN)📝 ПРИМЕРЫ:$(NC)"
	@echo "  make dev          # Запуск в development режиме"
	@echo "  make test         # Запуск всех тестов"
	@echo "  make deploy       # Подготовка к деплою"
	@echo "  make clean        # Очистка всех временных файлов"

# ==================== SETUP ====================
.PHONY: install
install: ## 📦 Установка зависимостей
	@echo "$(BLUE)📦 Установка зависимостей...$(NC)"
	$(PIP) install --upgrade pip wheel
	$(PIP) install -r $(REQUIREMENTS_FILE)
	@echo "$(GREEN)✅ Зависимости установлены$(NC)"

.PHONY: install-dev
install-dev: install ## 🛠️ Установка dev зависимостей
	@echo "$(BLUE)🛠️ Установка dev зависимостей...$(NC)"
	$(PIP) install pytest pytest-asyncio pytest-cov black flake8 mypy bandit safety
	@echo "$(GREEN)✅ Dev зависимости установлены$(NC)"

.PHONY: setup
setup: ## 🔧 Первоначальная настройка проекта
	@echo "$(BLUE)🔧 Настройка проекта...$(NC)"
	@if [ ! -f $(ENV_FILE) ]; then \
		echo "$(YELLOW)⚠️ Создание .env файла из шаблона...$(NC)"; \
		cp .env.template $(ENV_FILE); \
		echo "$(RED)❗ Отредактируйте .env файл перед продолжением!$(NC)"; \
	fi
	@make install-dev
	@make check-env
	@echo "$(GREEN)✅ Проект настроен$(NC)"

# ==================== РАЗРАБОТКА ====================
.PHONY: dev
dev: ## 🚀 Запуск в development режиме
	@echo "$(BLUE)🚀 Запуск development сервера...$(NC)"
	@make check-env
	$(DOCKER_COMPOSE) up -d --build
	@echo "$(GREEN)✅ Сервер запущен на http://localhost:8080$(NC)"
	@echo "$(YELLOW)📋 Команды:$(NC)"
	@echo "  make logs    - просмотр логов"
	@echo "  make stop    - остановка сервера"
	@echo "  make restart - перезапуск сервера"

.PHONY: run
run: ## ▶️ Запуск бота напрямую (без Docker)
	@echo "$(BLUE)▶️ Запуск бота...$(NC)"
	@make check-env
	$(PYTHON) $(MAIN_FILE)

.PHONY: logs
logs: ## 📋 Просмотр логов
	$(DOCKER_COMPOSE) logs -f bot

.PHONY: stop
stop: ## ⏹️ Остановка сервисов
	@echo "$(YELLOW)⏹️ Остановка сервисов...$(NC)"
	$(DOCKER_COMPOSE) down
	@echo "$(GREEN)✅ Сервисы остановлены$(NC)"

.PHONY: restart
restart: ## 🔄 Перезапуск сервисов
	@echo "$(YELLOW)🔄 Перезапуск сервисов...$(NC)"
	$(DOCKER_COMPOSE) restart
	@echo "$(GREEN)✅ Сервисы перезапущены$(NC)"

.PHONY: shell
shell: ## 🐚 Подключение к контейнеру
	$(DOCKER_COMPOSE) exec bot /bin/bash

.PHONY: db-shell
db-shell: ## 🗄️ Подключение к базе данных
	$(DOCKER_COMPOSE) exec postgres psql -U postgres -d telegram_bot

.PHONY: redis-shell
redis-shell: ## 📊 Подключение к Redis
	$(DOCKER_COMPOSE) exec redis redis-cli

# ==================== ТЕСТИРОВАНИЕ ====================
.PHONY: test
test: ## 🧪 Запуск всех тестов
	@echo "$(BLUE)🧪 Запуск тестов...$(NC)"
	$(PYTHON) -m pytest tests/ -v --tb=short
	@echo "$(GREEN)✅ Тесты завершены$(NC)"

.PHONY: test-cov
test-cov: ## 📊 Тесты с покрытием
	@echo "$(BLUE)📊 Тесты с анализом покрытия...$(NC)"
	$(PYTHON) -m pytest tests/ -v --cov=. --cov-report=term --cov-report=html
	@echo "$(GREEN)✅ Отчет сохранен в htmlcov/index.html$(NC)"

.PHONY: test-fast
test-fast: ## ⚡ Быстрые тесты (без медленных)
	$(PYTHON) -m pytest tests/ -v -m "not slow"

.PHONY: test-integration
test-integration: ## 🔗 Интеграционные тесты
	@echo "$(BLUE)🔗 Интеграционные тесты...$(NC)"
	$(DOCKER_COMPOSE) -f docker-compose.test.yml up --build --abort-on-container-exit
	$(DOCKER_COMPOSE) -f docker-compose.test.yml down

# ==================== КАЧЕСТВО КОДА ====================
.PHONY: format
format: ## 🎨 Форматирование кода
	@echo "$(BLUE)🎨 Форматирование кода...$(NC)"
	$(PYTHON) -m black . --line-length=100
	@echo "$(GREEN)✅ Код отформатирован$(NC)"

.PHONY: lint
lint: ## 🔍 Проверка стиля кода
	@echo "$(BLUE)🔍 Линтинг кода...$(NC)"
	$(PYTHON) -m flake8 . --max-line-length=100 --exclude=migrations,venv,env
	@echo "$(GREEN)✅ Линтинг завершен$(NC)"

.PHONY: type-check
type-check: ## 🔎 Проверка типов
	@echo "$(BLUE)🔎 Проверка типов...$(NC)"
	$(PYTHON) -m mypy $(MAIN_FILE) --ignore-missing-imports
	@echo "$(GREEN)✅ Проверка типов завершена$(NC)"

.PHONY: security
security: ## 🔒 Проверка безопасности
	@echo "$(BLUE)🔒 Проверка безопасности...$(NC)"
	$(PYTHON) -m safety check
	$(PYTHON) -m bandit -r . -x tests/ -f json -o bandit-report.json
	@echo "$(GREEN)✅ Проверка безопасности завершена$(NC)"

.PHONY: quality
quality: format lint type-check security ## ✨ Все проверки качества
	@echo "$(GREEN)✨ Все проверки качества завершены$(NC)"

# ==================== БАЗА ДАННЫХ ====================
.PHONY: db-init
db-init: ## 🗄️ Инициализация базы данных
	@echo "$(BLUE)🗄️ Инициализация базы данных...$(NC)"
	$(DOCKER_COMPOSE) exec postgres psql -U postgres -d telegram_bot -f /docker-entrypoint-initdb.d/init.sql
	@echo "$(GREEN)✅ База данных инициализирована$(NC)"

.PHONY: db-backup
db-backup: ## 💾 Создание бэкапа БД
	@echo "$(BLUE)💾 Создание бэкапа...$(NC)"
	@mkdir -p backups
	$(DOCKER_COMPOSE) exec -T postgres pg_dump -U postgres telegram_bot > backups/backup_$(shell date +%Y%m%d_%H%M%S).sql
	@echo "$(GREEN)✅ Бэкап создан в папке backups/$(NC)"

.PHONY: db-restore
db-restore: ## 🔄 Восстановление БД из бэкапа
	@echo "$(BLUE)🔄 Восстановление базы данных...$(NC)"
	@read -p "Введите имя файла бэкапа: " backup_file; \
	$(DOCKER_COMPOSE) exec -T postgres psql -U postgres telegram_bot < backups/$$backup_file
	@echo "$(GREEN)✅ База данных восстановлена$(NC)"

.PHONY: db-reset
db-reset: ## 🗑️ Сброс базы данных
	@echo "$(RED)⚠️ ВНИМАНИЕ: Это удалит ВСЕ данные!$(NC)"
	@read -p "Вы уверены? (y/N): " confirm; \
	if [ "$$confirm" = "y" ] || [ "$$confirm" = "Y" ]; then \
		$(DOCKER_COMPOSE) down -v; \
		$(DOCKER_COMPOSE) up -d postgres; \
		sleep 5; \
		make db-init; \
		echo "$(GREEN)✅ База данных сброшена$(NC)"; \
	else \
		echo "$(YELLOW)❌ Операция отменена$(NC)"; \
	fi

# ==================== ДЕПЛОЙ ====================
.PHONY: build
build: ## 🏗️ Сборка Docker образа
	@echo "$(BLUE)🏗️ Сборка Docker образа...$(NC)"
	$(DOCKER) build -t $(PROJECT_NAME):latest .
	@echo "$(GREEN)✅ Образ собран$(NC)"

.PHONY: prepare-deploy
prepare-deploy: quality test build ## 🚀 Подготовка к деплою
	@echo "$(BLUE)🚀 Подготовка к деплою...$(NC)"
	@echo "$(GREEN)✅ Готов к деплою!$(NC)"

.PHONY: deploy-staging
deploy-staging: prepare-deploy ## 🎭 Деплой на staging
	@echo "$(BLUE)🎭 Деплой на staging...$(NC)"
	./deploy.sh staging
	@echo "$(GREEN)✅ Staging обновлен$(NC)"

.PHONY: deploy-production
deploy-production: prepare-deploy ## 🌟 Деплой на production
	@echo "$(RED)⚠️ PRODUCTION DEPLOY$(NC)"
	@read -p "Вы уверены что хотите обновить production? (y/N): " confirm; \
	if [ "$$confirm" = "y" ] || [ "$$confirm" = "Y" ]; then \
		./deploy.sh production; \
		echo "$(GREEN)✅ Production обновлен$(NC)"; \
	else \
		echo "$(YELLOW)❌ Деплой отменен$(NC)"; \
	fi

# ==================== МОНИТОРИНГ ====================
.PHONY: health
health: ## 🏥 Проверка здоровья сервиса
	@echo "$(BLUE)🏥 Проверка здоровья...$(NC)"
	@curl -s http://localhost:8080/health | jq . || echo "$(RED)❌ Сервис недоступен$(NC)"

.PHONY: metrics
metrics: ## 📊 Показать метрики
	@echo "$(BLUE)📊 Метрики сервиса:$(NC)"
	@curl -s http://localhost:8080/metrics | jq . || echo "$(RED)❌ Метрики недоступны$(NC)"

.PHONY: stats
stats: ## 📈 Статистика бота
	@echo "$(BLUE)📈 Статистика бота:$(NC)"
	$(PYTHON) monitoring.py stats

.PHONY: monitor
monitor: ## 👁️ Мониторинг в реальном времени
	@echo "$(BLUE)👁️ Запуск мониторинга...$(NC)"
	@while true; do \
		clear; \
		echo "$(GREEN)🤖 Telegram Bot Monitor$(NC)"; \
		echo "$(YELLOW)⏰ $(shell date)$(NC)"; \
		echo ""; \
		make health 2>/dev/null; \
		echo ""; \
		make metrics 2>/dev/null; \
		sleep 30; \
	done

# ==================== УТИЛИТЫ ====================
.PHONY: clean
clean: ## 🧹 Очистка временных файлов
	@echo "$(BLUE)🧹 Очистка проекта...$(NC)"
	find . -type f -name "*.pyc" -delete
	find . -type d -name "__pycache__" -delete
	find . -type d -name "*.egg-info" -exec rm -rf {} +
	find . -type f -name "*.log" -delete
	rm -rf .pytest_cache/
	rm -rf htmlcov/
	rm -rf .coverage
	rm -rf dist/
	rm -rf build/
	$(DOCKER) system prune -f
	@echo "$(GREEN)✅ Проект очищен$(NC)"

.PHONY: clean-all
clean-all: clean ## 🗑️ Полная очистка (включая Docker)
	@echo "$(BLUE)🗑️ Полная очистка...$(NC)"
	$(DOCKER_COMPOSE) down -v --remove-orphans
	$(DOCKER) system prune -af --volumes
	@echo "$(GREEN)✅ Полная очистка завершена$(NC)"

.PHONY: update
update: ## 🔄 Обновление зависимостей
	@echo "$(BLUE)🔄 Обновление зависимостей...$(NC)"
	$(PIP) install --upgrade pip
	$(PIP) install -r $(REQUIREMENTS_FILE) --upgrade
	@echo "$(GREEN)✅ Зависимости обновлены$(NC)"

.PHONY: backup
backup: db-backup ## 💾 Полный бэкап
	@echo "$(BLUE)💾 Создание полного бэкапа...$(NC)"
	@mkdir -p backups
	tar -czf backups/full_backup_$(shell date +%Y%m%d_%H%M%S).tar.gz \
		--exclude='.git' \
		--exclude='__pycache__' \
		--exclude='venv' \
		--exclude='node_modules' \
		--exclude='*.log' \
		.
	@echo "$(GREEN)✅ Полный бэкап создан$(NC)"

.PHONY: check-env
check-env: ## ✅ Проверка переменных окружения
	@echo "$(BLUE)✅ Проверка окружения...$(NC)"
	@if [ ! -f $(ENV_FILE) ]; then \
		echo "$(RED)❌ Файл .env не найден!$(NC)"; \
		echo "$(YELLOW)💡 Выполните: make setup$(NC)"; \
		exit 1; \
	fi
	@echo "$(GREEN)✅ Окружение настроено$(NC)"

.PHONY: info
info: ## ℹ️ Информация о проекте
	@echo "$(BLUE)ℹ️ Информация о проекте:$(NC)"
	@echo "  Название: $(PROJECT_NAME)"
	@echo "  Python: $(shell $(PYTHON) --version)"
	@echo "  Docker: $(shell $(DOCKER) --version)"
	@echo "  Статус сервисов:"
	@$(DOCKER_COMPOSE) ps 2>/dev/null || echo "    Сервисы не запущены"

# ==================== ДОКУМЕНТАЦИЯ ====================
.PHONY: docs
docs: ## 📚 Генерация документации
	@echo "$(BLUE)📚 Генерация документации...$(NC)"
	@echo "$(YELLOW)💡 Документация находится в README.md$(NC)"

.PHONY: docs-serve
docs-serve: ## 🌐 Локальный сервер документации
	@echo "$(BLUE)🌐 Запуск сервера документации...$(NC)"
	$(PYTHON) -m http.server 8000 --directory docs/

# По умолчанию показываем help
.DEFAULT_GOAL := help

# ==================== СПЕЦИАЛЬНЫЕ ЦЕЛИ ====================
# Эти цели не создают файлы
.PHONY: all
all: setup quality test build ## 🎯 Полная сборка проекта

# Автоматические проверки перед коммитом
.PHONY: pre-commit
pre-commit: format lint test ## 🔍 Проверки перед коммитом
	@echo "$(GREEN)✅ Готово к коммиту!$(NC)"

# Быстрая проверка изменений
.PHONY: check
check: format lint test-fast ## ⚡ Быстрая проверка
	@echo "$(GREEN)✅ Быстрая проверка завершена$(NC)"
